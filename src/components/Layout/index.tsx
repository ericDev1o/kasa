import Header from "../Header";
import Main from "../Main";
import Footer from "../Footer";

const Layout = () => {
    return (
        <body>
            <Header />
            <Main />
            <Footer />
        </body>
    );
};

export default Layout;
